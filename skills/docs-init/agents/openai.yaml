interface:
  display_name: "DOCS Init"
  short_description: "Inicializa e roteia o ambiente DOCS"
  default_prompt: "Use $docs-init para preparar, validar ou rotear uma tarefa dentro do metodo DOCS neste repositorio."

policy:
  allow_implicit_invocation: true
