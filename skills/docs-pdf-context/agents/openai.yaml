interface:
  display_name: "DOCS PDF Context"
  short_description: "Prepara contexto DOCS a partir de PDF"
  default_prompt: "Use $docs-pdf-context para extrair e sintetizar um PDF de requisitos em README.md, context.json e tables.json quando houver tabelas."

policy:
  allow_implicit_invocation: false
