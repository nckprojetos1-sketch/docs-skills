# reports

O `reports/` da raiz do subplano consolida:

- o que foi executado nos `xx-sddd`;
- evidências (arquivos afetados, validações, saídas);
- handoffs para outras frentes.

Os reports **não** ficam dentro de `01-sddd/`. Nomeie por data: `AAAA-MM-DD__<assunto>.md`.
